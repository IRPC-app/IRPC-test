# Required for the AI Da'ee feature (api/ask-daee.js) to answer real questions.
# Get a key from https://console.anthropic.com/ and set it in your deployment
# platform's environment variables (e.g. Vercel Project Settings > Environment Variables).
ANTHROPIC_API_KEY=
